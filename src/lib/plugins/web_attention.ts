import PamTracker from "..";
import { Plugin } from "../core/plugin";

export class WebSttenTionPlugin extends Plugin {
  // override initPlugin(pam: PamTracker): void {
  //   pam.hook.onStartup();
  // }
}
